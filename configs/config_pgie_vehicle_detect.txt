[property]
gpu-id=0

# YOLOv11 custom: detect đủ 14 classes gốc.
# Edge profile: tracker giữ ID mượt giữa các frame, PGIE chạy cách frame để tăng FPS.
onnx-file=/workspace/ds_lpr_v2/models/vehicle_parking_detect.onnx
model-engine-file=/workspace/ds_lpr_v2/models/vehicle_parking_detect.onnx_b1_gpu0_fp16.engine
labelfile-path=/workspace/ds_lpr_v2/models/labels_vehicle.txt

# overridden at runtime by app_lpr_v2.py to match num_sources
batch-size=1
network-mode=2
network-type=0
process-mode=1
gie-unique-id=1
interval=1

num-detected-classes=14

infer-dims=3;640;640
maintain-aspect-ratio=1
symmetric-padding=1

net-scale-factor=0.00392156862745098
model-color-format=0

custom-lib-path=/workspace/ds_lpr_v2/custom_parser/libnvds_infercustom_yolov11_flat.so
parse-bbox-func-name=NvDsInferParseCustomYoloV11Flat

cluster-mode=2

[class-attrs-all]
pre-cluster-threshold=0.35
nms-iou-threshold=0.45
topk=300

# License plate full-frame boxes are small; keep this lower while the rest of
# classes stay at the normal threshold.
[class-attrs-13]
pre-cluster-threshold=0.10
nms-iou-threshold=0.45
topk=40
